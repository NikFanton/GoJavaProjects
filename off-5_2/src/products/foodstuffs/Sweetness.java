package products.foodstuffs;

import products.components.ShelfLife;

public class Sweetness extends Food {
    public Sweetness(int id, int name, int quantity, float price, ShelfLife shelfLife) {
        super(id, name, quantity, price, shelfLife);
    }
}
